apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: boomi-storage
  namespace: boomi-production
  labels:
    app.kubernetes.io/name: boomi-molecule-prod
    app.kubernetes.io/component: storage
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
